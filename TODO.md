# GsGit: План работы по сборке и исправлению безопасности

## Текущие задачи

- [x] **Исправление ложного детектирования среды (False Positive)**
  - Изменен алгоритм расшифровки системных строк в `security.c` (используется статический `BUILD_KEY` вместо нестабильного `g_hw_key()`).
  - Уникальный аппаратный ключ устройства сохранен для безопасного шифрования токена GitHub.

- [ ] **Связь с репозиторием Git и Пуш изменений**
  - Инициализировать git в `gsgit_latest` (в процессе).
  - Привязать к репозиторию `git@github.com:lkolholk-ctrl/GlassFiles-1.git`.
  - Запушить изменения в ветку на GitHub.

- [ ] **Синхронизация и сборка на VPS**
  - Подключиться к `myvps` по SSH.
  - Перейти в папку проекта `/root/GlassFiles-1`.
  - Стянуть изменения (`git pull origin`).
  - Выполнить команду сборки релизного APK (`./gradlew assembleRelease`).

- [ ] **Доставка сборки на телефон**
  - Скопировать скомпилированный APK-файл с VPS на телефон в папку `/storage/emulated/0/Download/`.
